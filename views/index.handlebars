{{#each articles}}
  <ul class="collapsible" data-collapsible="accordion">

    <!-- Article Drop Down -->
    <li class="section">
      <div style="background-color: white;" class="collapsible-header grey lighten-2">
        <div style="clear: left; line-height: 110%; padding: 10px 0 0 5px;"><b>{{this.title}}</b></div>
      </div>
      <div class="collapsible-body" style="background-color: white;">
        <p>
          {{this.summary}}

          <br><br>

          <i>Please read the full article <a href="{{this.link}}" target="_new">here</a>.</i>
        </p>
          
      </div>
    </li>


    <!-- Add Comments Drop Down -->
    <li class="section">
      <div class="collapsible-header">{{!-- <i class="material-icons">comment</i>  --}}
      Add a Comment</div>
      <div class="collapsible-body" style="background-color: white;">

        <form id="form-add-{{_id}}" action="add/comment/{{_id}}" method="post" style="background-color: white;">

          <!-- Author Name Input -->
          <div class="row">
            <div class="input-field col s2"></div>
            <div class="input-field col s8">
              <input id="author_name" name="name" type="text" class="validate">
              <label for="author_name"> Your Name:</label>
            </div>
          </div>
          
          <!-- Comment Input -->
          <div class="row">
            <div class="input-field col s2"></div>
            <div class="input-field col s8">
              <textarea id="comment_box" name="comment" class="materialize-textarea"></textarea>
              <label for="comment_box"> Your Reaction:</label>
            </div>
          </div>

            <!-- Submit Button -->
            <div class="row">
              <div class="input-field col s2"></div>
              <div class="input-field col s8">
              <center><input class="btn add-comment-button" data-id="{{_id}}" type="submit" value="Submit"></center>
              </div>
            </div>
          
        </form>

      </div>
    </li>


    <!-- View Comments Drop Down -->
    <li class="section">
      <div class="collapsible-header">{{!-- <i class="material-icons">thumbs_up_down</i> --}} View Comments</div>
      <div class="collapsible-body" style="background-color: white;">
      
        {{!-- Check if the article has comments --}}
        {{#if comments}}

          {{!-- Article has comments so iterate over them  --}}
          <ul class="collection">
            {{#each comments}}
            
              <li class="collection-item">

                {{!-- Add Author Name --}}
                <b>{{author}}</b> 

                {{!-- Add a Delete Button --}}
                <span class="badge">
                  <form id="form-delete-{{_id}}" action="remove/comment/{{_id}}" method="post">
                    <input class="btn-small delete-comment-button" data-id="{{_id}}" type="submit" value="Delete" style="color: white; background-color: red; border-color: red;">
                  </form>
                </span>

                <br>

                {{!-- Add the Content of the Comment --}}
                {{content}}

              </li> 
            {{/each}}
          </ul>
        
        {{!-- No comments, so display message --}}
        {{else}}
          <p>No Comments!!! </p>
        {{/if}}
            
        
      </div>
    </li>

  </ul>
{{/each}}